{
  "name": "mandrachain",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "babel ./dev/*.js -d dist",
    "start": "npm run build && node dist/index.js",
    "start:api": "npm run build && nodemon --watch dist -e js dist/api.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Felipe Del Valle",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.12.10",
    "@babel/core": "^7.12.10",
    "@babel/preset-env": "^7.12.11"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.7",
    "sha256": "^0.2.0",
    "uuid": "^8.3.2"
  }
}
